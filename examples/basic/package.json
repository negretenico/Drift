{
  "name": "basic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all": "npm run build && negretenico-sw public",
    "preview": "vite preview"
  },
  "dependencies": {
    "@negretenico/sw": "*"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
